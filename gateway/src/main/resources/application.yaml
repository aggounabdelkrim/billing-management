server:
  port:9090

spring:
  application:
    name: Billing management gateway
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB